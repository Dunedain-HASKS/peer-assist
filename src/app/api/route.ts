import { NextRequest, NextResponse } from "next/server";
import { closeQuestion, getQuestionThread, postCommentToQuestion, voteQuestion } from "@/server/services/question.service";

export async function POST(request: NextRequest, context: any) {
  
}; 
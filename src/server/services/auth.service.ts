export const verifyToken = ({ token }: { token: string }) => {
    // 
};

export const createTokenWithUserName = ({ username, password }: { username: string, password: string }) => {
    //
};

export const createTokenWithEmail = ({ email, password }: { email: string, password: string }) => {
    //
};

export const invalidateToken = ({ token }: { token: string }) => {
    // 
};




